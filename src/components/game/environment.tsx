"use client";

/**
 * Umgebungs-Deko für das Blitzverkauf-Spiel (Bäume, Laternen, Sterne,
 * Stadt-Silhouette). Aktuell bewusst ein leerer Platzhalter mit fixem
 * Interface — wird im Premium-Ausbau befüllt, ohne dass die Szene
 * (game-canvas.tsx) angepasst werden muss.
 */
export function Environment({}: { length: number; reduceMotion: boolean }) {
  return null;
}
